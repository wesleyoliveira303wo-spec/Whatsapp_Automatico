export const logger = {
  info: (_msg: string, _meta?: Record<string, unknown>) => {
    // Placeholder logger – in real app replace with Winston/Pino
  },
  error: (_msg: string, _meta?: Record<string, unknown>) => {
    // Placeholder logger – in real app replace with Winston/Pino
  },
  debug: (_msg: string, _meta?: Record<string, unknown>) => {
    // Placeholder logger – in real app replace with Winston/Pino
  },
};
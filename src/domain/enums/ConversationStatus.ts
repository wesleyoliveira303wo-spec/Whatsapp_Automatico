export enum ConversationStatus {
  NEW = 'new',
  ACTIVE = 'active',
  WAITING_CUSTOMER = 'waiting_customer',
  WAITING_AGENT = 'waiting_agent',
  RESOLVED = 'resolved',
  ARCHIVED = 'archived',
  SPAM = 'spam',
}

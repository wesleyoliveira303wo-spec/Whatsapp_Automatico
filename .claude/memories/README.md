# Claude Memories

The `.claude/memories/` folder stores persistent knowledge about the project, user preferences, and past decisions. Each memory is a markdown file with frontmatter defining its type (`user`, `feedback`, `project`, `reference`). See the **Memory Guide** in the main `CLAUDE.md` for conventions.

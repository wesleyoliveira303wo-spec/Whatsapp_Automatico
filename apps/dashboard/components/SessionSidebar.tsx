/**
 * Depreciado no Redesign 2026-08-05 (R2) — substituído por
 * `components/SessionRail.tsx` (rail de ícones w-14, em vez da sidebar w-64
 * com rótulos). Arquivo preservado como re-export só porque o ambiente não
 * permite apagar arquivos (mesmo padrão já usado para `SessionListItem.tsx`,
 * M6G) — nenhum código novo deve importar daqui.
 */
export { default } from './SessionRail';
